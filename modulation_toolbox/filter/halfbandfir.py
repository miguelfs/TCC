def validateParseInput(N: str, fp, varagin):
    minOrderFlag = 1 if N == 'minorder' else 0



def halfbandfir(N: str, fp, varagin) :
    minOrderFlag, lowpassFlag, msg = validateParseInput(N, fp: float, varagin)
